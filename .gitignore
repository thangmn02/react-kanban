# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.env
.env.local
.env.*.local
*.local

# Local agent/config and manually managed Supabase workspace files
.agent/
supabase/
.kiro/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Third-party Balatro mod sources (GPL-3.0). Only the sliced atlases under
# public/arcana/atlas are used by the app; the full mod folders (Lua + large
# PNGs) should not be uploaded.
public/DX-Tarots-master/
public/JellyMod-main/

# Raw Hugging Face dataset — regenerate the normalized JSON via
# scripts/pull_hf_tarot.py (the generated public/arcana/data/*.json is committed
# because it is a runtime asset).
public/tarot_readings.parquet

# Project docs and local data/build scripts — kept locally, not uploaded.
docs/
scripts/
